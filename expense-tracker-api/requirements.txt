# Direct dependencies for the Expense Tracker API.
# To regenerate the lock file after changing this file:
#   python -m venv /tmp/lockenv && /tmp/lockenv/bin/pip install -r requirements.txt && /tmp/lockenv/bin/pip freeze > requirements.lock
#
# DEP-05: uvicorn WITHOUT [standard] — watchfiles (needed for --reload) is a dev dependency only.
fastapi==0.115.0
uvicorn==0.30.6
sqlalchemy==2.0.36
python-dotenv==1.0.1
pydantic>=2.0,<3      # DEP-01: explicit direct dependency; FastAPI requires pydantic v2
alembic>=1.13,<2      # DEBT-01: Alembic for proper schema migrations
slowapi>=0.1.9,<1     # SEC-05: rate limiting
