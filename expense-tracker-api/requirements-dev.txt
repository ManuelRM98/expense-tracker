-r requirements.txt
# DEP-05: uvicorn[standard] installs watchfiles (needed for --reload).
# Only in dev — production uses plain uvicorn from requirements.txt.
uvicorn[standard]==0.30.6
pytest>=8.0
httpx>=0.27
