# Backend environment variables.
# Copy this file to .env (gitignored) and fill in real values. NEVER commit .env.

# ── Database ──────────────────────────────────────────────────────────────────
# SQLite (default — zero config). database.py falls back to this if DATABASE_URL
# is unset, so leaving it empty keeps local SQLite behavior.
DATABASE_URL=sqlite:///./expense_tracker.db

# PostgreSQL / Supabase — use the dashboard "Session pooler" string (port 5432,
# IPv4-friendly). The 6543 "transaction pooler" breaks SQLAlchemy prepared
# statements; do not use it. database.py rewrites postgresql:// to
# postgresql+psycopg:// automatically. Keep ?sslmode=require.
# DATABASE_URL=postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres?sslmode=require

# ── AUTH-01: Supabase JWT verification ────────────────────────────────────────
# Project URL used to derive the JWKS endpoint and validate the `iss` claim.
SUPABASE_URL=https://<project-ref>.supabase.co

# ── FEAT-14: Supabase service-role key (admin account deletion only) ──────────
# Required for DELETE /account/me to remove the Supabase Auth identity.
# Find this in Supabase dashboard → Settings → API → service_role key.
# NEVER expose this to the frontend — it is used server-side only.
SUPABASE_SERVICE_ROLE_KEY=

# ── API auth (SEC-02) ───────────────────────────────────────────────────────────
# When set, all routes (except /, /docs, /openapi.json, /redoc) require header
# `X-API-Key: <this value>`. The frontend must send the SAME value via
# VITE_API_KEY. Generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Leave empty to disable auth.
API_KEY=
